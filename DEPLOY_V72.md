# v7.2 배포 안내

이 ZIP의 파일들을 Git 저장소 `png` 폴더 안에 덮어쓴 뒤 실행하세요.

```powershell
git status
git add .
git commit -m "Improve product package background removal"
git push origin main
```

성공 후 `https://moonoverlap.github.io/png/`에서 Ctrl+F5로 새로고침하세요.
