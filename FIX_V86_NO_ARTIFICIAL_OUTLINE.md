# v86 No Artificial Outline

## 문제

v83~v85에서 피사체 보호를 위해 박스 잠금/보호 마스크를 강하게 적용하면서, 실제 피사체가 아닌 흰 카드 배경 또는 회색 박스선이 외곽선처럼 남는 문제가 발생했습니다.

## 원인

`applyCatalogPackageBoxLock()`가 박스 안쪽 전체를 `alpha=255`로 강제 채우고 있었습니다. 이 방식은 흰 포장지를 보호하는 데는 도움이 되지만, 상품 바깥의 얇은 흰 배경/회색 경계선까지 피사체로 만들 수 있습니다.

## 수정

- 박스 내부를 강제로 불투명 처리하지 않음
- 박스 밖 배경만 제거하는 trim-only 방식으로 변경
- 피사체 모양은 AI/색상/보호 마스크 결과를 유지
- `suppressArtificialEnvelopeEdges()` 추가
- 투명 배경과 맞닿은 저채도 흰색/회색 직선 잔여물만 제거
- 식품, 로고, 인쇄, 컬러 포장지는 제거하지 않도록 보호

## 결과 방향

피사체는 보호하지만, 앱이 새로 만든 것처럼 보이는 외곽선/사각 테두리는 만들지 않습니다.
