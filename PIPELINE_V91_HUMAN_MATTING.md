# v91 Human / Person Matting Mode

## 목적

API 없이 브라우저 로컬 환경에서 사람 사진도 배경 제거가 가능하도록 v90의 2단계 파이프라인에 인물 전용 matting 분기를 추가했습니다.

## 변경 요약

- 피사체 유형 선택 추가: 자동 감지 / 인물·의류 / 상품·패키지 / 음식·접시
- 인물 사진 자동 판정 함수 추가: `looksLikeHumanSubject()`
- 인물 사진에서는 상품 패키지 보정, 사각 외곽 잠금, 단색 배경 flood-fill을 기본 우선 경로로 사용하지 않음
- 머리카락, 얼굴, 손, 소매, 가방끈, 종이 태그처럼 얇은 경계가 끊기지 않도록 segmentation threshold와 matting curve 조정
- 인물 모드 선택 시 사용자가 단색 배경 빠른 제거를 선택해도 2단계 AI matting을 우선 사용

## 처리 방향

```text
1단계: 로컬 AI Segmentation
2단계: 피사체 유형 판정
3단계: 인물이면 Human Matting 파라미터 적용
4단계: 원본 색상 보존 + 알파 경계 정제
```

## 사용 권장

사람 사진은 `피사체 유형`을 `인물/의류 · 머리카락 보호`로 선택하면 자동 판정보다 안정적입니다.
