# v90 API-free Segmentation + Matting 파이프라인

## 변경 목표

개별 상품 예외 처리에 의존하지 않고, 배경 제거를 두 단계로 분리했습니다.

1. **Segmentation**: 로컬 AI 모델이 피사체의 대략적인 전경 마스크를 만듭니다.
2. **Matting**: 원본 해상도에서 경계선 밴드를 다시 계산해 알파를 정제합니다.

## 핵심 변경

- 기본 엔진을 `로컬 2단계 · Segmentation + Matting`으로 변경
- 외부 API / Hugging Face 서버 URL 입력 제거
- 상품 패키지, 비닐팩, 음식 사진별 강제 보정 로직을 기본 경로에서 제외
- 기존 보정 경로는 `로컬 AI 원본 마스크 · 보조`에 남김
- 경계선 정제는 다음 기준으로 수행
  - AI 세그멘테이션 알파
  - 전경/배경 확정 시드 거리 변환
  - 원본 픽셀 색상과 배경 추정색의 차이
  - 원본 이미지 경계선 강도
  - 경계 연결 저알파 잔여물 제거

## 기대 효과

- 한 상품을 고치면 다른 상품이 깨지는 문제 감소
- 사각 외곽선/카드 배경 생성 억제
- 흰 포장지, 비닐, 음식, 신발 등 서로 다른 이미지 유형을 같은 기본 파이프라인으로 처리

## 한계

브라우저 로컬 모델의 세그멘테이션 자체가 피사체를 크게 놓치면 Matting만으로 완전히 복구할 수 없습니다. 하지만 기존처럼 후처리 규칙이 배경을 다시 살리거나 피사체를 뜯는 문제는 줄어듭니다.
